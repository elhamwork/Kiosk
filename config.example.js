// Copy this file to config.js and fill in your own key.
// config.js is gitignored — never commit a real API key.
//
// Get a free key at https://console.groq.com
//
// SECURITY NOTE: this kiosk runs entirely client-side, so this key is
// visible to anyone using the kiosk (page source / devtools network tab).
// That's an accepted tradeoff for this project — do not reuse a key here
// that's shared with other, higher-trust systems, and rotate it if you
// suspect it's been exposed.
const GROQ_API_KEY = "YOUR_GROQ_API_KEY_HERE";
const GROQ_MODEL = "llama-3.3-70b-versatile";
